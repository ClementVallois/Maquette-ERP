/**
 * Re-exported from `@erp/contracts` rather than hand-duplicated — the single description lives
 * in `packages/contracts/src/pre-facturier.ts`, shared with
 * `apps/api/src/routes/pre-facturier.ts`.
 */
export type {
  DeclineReason,
  PreFacturierCraRow,
  PreFacturierInvoiceRow,
  PreFacturierResponse,
  PreFacturierSummary,
} from '@erp/contracts';
