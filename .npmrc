engine-strict=true
ignore-scripts=true

# 7 days, in minutes. Quarantines freshly published versions.
minimumReleaseAge=10080

# `saveExact` is NOT read from here in pnpm 11 — it lives in pnpm-workspace.yaml.
