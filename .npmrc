engine-strict=true
ignore-scripts=true

# `saveExact` and `minimumReleaseAge` are NOT read from here in pnpm 11 — they live in
# pnpm-workspace.yaml. `minimumReleaseAge` sat in this file from Phase 0 to 24/08/2026 and was
# read by nothing the whole time: `pnpm config get minimumReleaseAge` answered `undefined`.
